# Public URL of the Funversarial CV demo (Next.js injects NEXT_PUBLIC_* at build time).
# Set this in Vercel → Project → Environment Variables for Production (and Preview if previews should use a stable CV URL).
# The lobby "Launch Demo" button links here.
NEXT_PUBLIC_CV_APP_URL=https://funversarial-cv.vercel.app
